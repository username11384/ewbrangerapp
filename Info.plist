<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Bundle identity (substituted by build settings) -->
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundleDisplayName</key>
    <string>LLRangers</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>

    <!-- Location -->
    <key>NSLocationWhenInUseUsageDescription</key>
    <string>Lama Lama Rangers records the GPS location of each invasive plant sighting so it can be mapped and tracked over time.</string>

    <!-- Bluetooth / Local Network (MultipeerConnectivity end-of-day sync) -->
    <key>NSLocalNetworkUsageDescription</key>
    <string>Lama Lama Rangers uses nearby network access to sync sighting data with other rangers at end of day, without needing the internet.</string>
    <key>NSBonjourServices</key>
    <array>
        <string>_yac-ranger._tcp</string>
        <string>_yac-ranger._udp</string>
    </array>

    <!-- Camera (sighting photos) -->
    <key>NSCameraUsageDescription</key>
    <string>Lama Lama Rangers uses the camera to photograph invasive plant infestations when logging a sighting.</string>

    <!-- Microphone (voice notes) -->
    <key>NSMicrophoneUsageDescription</key>
    <string>Lama Lama Rangers uses the microphone to record optional voice notes when logging a sighting.</string>

    <!-- UI -->
    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <false/>
    </dict>
    <key>UIApplicationSupportsIndirectInputEvents</key>
    <true/>
    <key>UILaunchScreen</key>
    <dict/>
    <key>UISupportedInterfaceOrientations~iphone</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
</dict>
</plist>
